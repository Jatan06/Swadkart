import Menus.MainMenu;
class Main {
    public static void main(String[] args) {
        MainMenu.show(); // This function of MainMenu calls show (method) that displays register and login interface.
    }
}